import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.otherspe",
  appName: "OthersPe",
  webDir: "build",
  server: {
    androidScheme: "https",
  },
};

export default config;
